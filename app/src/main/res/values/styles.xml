<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/lightBlue</item>
        <item name="colorPrimaryDark">@color/darkBlue</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="buttonStyle">@style/smallSizeButton</item>
        <item name="editTextStyle">@style/smallSizeEditText</item>
    </style>
    <style name="smallSizeButton" parent="Widget.AppCompat.Button">
        <item name="android:textSize">11dp</item>
        <item name="android:paddingLeft">6dp</item>
    </style>
    <style name="smallSizeEditText" parent="Widget.AppCompat.EditText">
        <item name="android:textSize">16dp</item>
        <item name="android:fontFamily">Letter Gothic Std</item>
    </style>
</resources>
